services:
  drop:
    build: .
    container_name: drop
    restart: unless-stopped
    environment:
      - PORT=18790
      - CAPTURE_DIR=/raw
    volumes:
      - /home/deploy/wiki/raw/library/captures:/raw
    ports:
      - "${DROP_BIND_IP:-127.0.0.1}:18790:18790"
